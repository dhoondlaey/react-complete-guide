name: Deploy React App to EC2

on:
  push:
    branches: [ "deployment-testing" ]
  pull_request:
    branches: [ "deployment-testing" ]
env:
  AWS_REGION: us-east-2
  DOCKER_TAG: 0.1
  DOCKER_REGISTRY: docker.io
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

jobs:
   
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x]
    steps:
    - uses: actions/checkout@v3
    - name: Log Secret
      run: echo "${{ vars.AWS_INSTANCE_USER }}"

#     - name: Use Node.js ${{ matrix.node-version }}
#       uses: actions/setup-node@v3
#       with:
#         node-version: ${{ matrix.node-version }}
#         cache: 'npm'
#     - run: npm ci
#     - run: npm run build --if-present


#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v2
#     - name: Login to Docker Hub
#       uses: docker/login-action@v2
#       with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
         
#     - name: Build and push
#       uses: docker/build-push-action@v4
#       with:
#           push: true
#           tags: iampsjain/react-complete-guide:latest

#     - name: Deploy to my EC2 instance
#       uses: easingthemes/ssh-deploy@v2.1.5
#       env:
#           SSH_PRIVATE_KEY: ${{ secrets.ECS_SSHKEY }}
#           SOURCE: "./"
#           REMOTE_HOST: ${{ secrets.AWS_HOST_NAME }}
#           REMOTE_USER: ${{ vars.AWS_INSTANCE_USER }}
#           TARGET: ${{ vars.AWS_APPLICATION_PATH }}
